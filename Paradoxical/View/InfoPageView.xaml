<UserControl
    x:Class="Paradoxical.View.InfoPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:view="clr-namespace:Paradoxical.View"
    xmlns:viewmodel="clr-namespace:Paradoxical.ViewModel"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodel:InfoPageViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <materialDesign:Card
        Margin="20,10"
        Padding="8"
        materialDesign:ElevationAssist.Elevation="Dp4">
        <ScrollViewer
            VerticalScrollBarVisibility="Hidden"
            HorizontalScrollBarVisibility="Disabled">
            <Grid
                Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="*"
                        MinWidth="100" />
                    <ColumnDefinition
                        Width="*"
                        MinWidth="100" />
                    <ColumnDefinition
                        Width="*"
                        MinWidth="100" />
                    <ColumnDefinition
                        Width="*"
                        MinWidth="100" />
                    <ColumnDefinition
                        Width="*"
                        MinWidth="100" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                </Grid.RowDefinitions>

                <TextBox
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Margin="4"
                    Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Context.Info.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="Mod Name" />

                <TextBox
                    Grid.Row="0"
                    Grid.Column="3"
                    Grid.ColumnSpan="1"
                    Margin="4"
                    Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Context.Info.Version, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="Mod Version" />

                <TextBox
                    Grid.Row="0"
                    Grid.Column="4"
                    Grid.ColumnSpan="1"
                    Margin="4"
                    Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Context.Info.GameVersion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="Game Version" />

                <TextBox
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="4"
                    Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Context.Info.EventNamespace, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="Namespace"
                    materialDesign:HintAssist.HelperText="Used for events, file names, etc." />
            </Grid>
        </ScrollViewer>
    </materialDesign:Card>

</UserControl>
